# Root Folder was initially a React project, therefore this gitignore file is needed at the root and the client folder to avoid Git/GitHub tracking unneeded code, keeping our repo light!

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build artifacts
dist
dist-ssr
.vite/

# Cypress files to ignore
cypress/screenshots/
cypress/videos/
cypress/downloads/
.cypress_cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Hidden file system in every folder for Finder, does nothing but clutter our code/repo
.DS_Store
.env
